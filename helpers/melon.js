const request = require('request');
const melon = require('melon-chart-api');

let getSongs = (artistName, callback) => {
	// console.log('ISTHISWORKING', artistName);

}

module.exports.getSongs = getSongs;